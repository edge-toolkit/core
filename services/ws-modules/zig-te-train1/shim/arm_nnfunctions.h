/* WASM build stub — shadows CMSIS-NN arm_nnfunctions.h.
 * TinyEngine provides its own kernel implementations; this header is only
 * needed for type definitions which arm_compat.h already supplies. */
#pragma once
#include "arm_compat.h"
