//! Wasmtime host bindings for the `runner` world.
//!
//! GENERATED by `mise run gen:wasmtime-bindings` (et-int-gen) from `generated/specs/wit`. Do not edit --
//! change the WIT or the generator instead, and regenerate. `gen-specs-check` fails when this drifts.
#![allow(
    unsafe_code,
    unused_variables,
    clippy::all,
    clippy::pedantic,
    clippy::restriction,
    clippy::missing_const_for_fn,
    clippy::too_long_first_doc_paragraph,
    clippy::type_repetition_in_bounds,
    clippy::use_self,
    reason = "generated from WIT by wasmtime-internal-wit-bindgen; we don't control its output. The group
    allows cover most of it, but Cargo's [lints] table passes -D per group on the command line, which an
    in-source group allow does not override -- so the four lints that still fire are named individually.
    A wasmtime bump that emits a fifth fails clippy until this list grows, which is the loud direction."
)]
#[doc(hidden)]
pub use super::host::wasi_keyvalue::Bucket as __with_name0;
/// Auto-generated bindings for a pre-instantiated version of a
/// component which implements the world `runner`.
///
/// This structure is created through [`RunnerPre::new`] which
/// takes a [`InstancePre`](wasmtime::component::InstancePre) that
/// has been created through a [`Linker`](wasmtime::component::Linker).
///
/// For more information see [`Runner`] as well.
pub struct RunnerPre<T: 'static> {
    instance_pre: wasmtime::component::InstancePre<T>,
    indices: RunnerIndices,
}
impl<T: 'static> Clone for RunnerPre<T> {
    fn clone(&self) -> Self {
        Self {
            instance_pre: self.instance_pre.clone(),
            indices: self.indices.clone(),
        }
    }
}
impl<_T: 'static> RunnerPre<_T> {
    /// Creates a new copy of `RunnerPre` bindings which can then
    /// be used to instantiate into a particular store.
    ///
    /// This method may fail if the component behind `instance_pre`
    /// does not have the required exports.
    pub fn new(
        instance_pre: wasmtime::component::InstancePre<_T>,
    ) -> wasmtime::Result<Self> {
        let indices = RunnerIndices::new(&instance_pre)?;
        Ok(Self { instance_pre, indices })
    }
    pub fn engine(&self) -> &wasmtime::Engine {
        self.instance_pre.engine()
    }
    pub fn instance_pre(&self) -> &wasmtime::component::InstancePre<_T> {
        &self.instance_pre
    }
    /// Instantiates a new instance of [`Runner`] within the
    /// `store` provided.
    ///
    /// This function will use `self` as the pre-instantiated
    /// instance to perform instantiation. Afterwards the preloaded
    /// indices in `self` are used to lookup all exports on the
    /// resulting instance.
    pub fn instantiate(
        &self,
        mut store: impl wasmtime::AsContextMut<Data = _T>,
    ) -> wasmtime::Result<Runner> {
        let mut store = store.as_context_mut();
        let instance = self.instance_pre.instantiate(&mut store)?;
        self.indices.load(&mut store, &instance)
    }
}
impl<_T: Send + 'static> RunnerPre<_T> {
    /// Same as [`Self::instantiate`], except with `async`.
    pub async fn instantiate_async(
        &self,
        mut store: impl wasmtime::AsContextMut<Data = _T>,
    ) -> wasmtime::Result<Runner> {
        let mut store = store.as_context_mut();
        let instance = self.instance_pre.instantiate_async(&mut store).await?;
        self.indices.load(&mut store, &instance)
    }
}
/// Auto-generated bindings for index of the exports of
/// `runner`.
///
/// This is an implementation detail of [`RunnerPre`] and can
/// be constructed if needed as well.
///
/// For more information see [`Runner`] as well.
#[derive(Clone)]
pub struct RunnerIndices {
    interface0: exports::et::ws_wasi::entry::GuestIndices,
}
/// Auto-generated bindings for an instance a component which
/// implements the world `runner`.
///
/// This structure can be created through a number of means
/// depending on your requirements and what you have on hand:
///
/// * The most convenient way is to use
///   [`Runner::instantiate`] which only needs a
///   [`Store`], [`Component`], and [`Linker`].
///
/// * Alternatively you can create a [`RunnerPre`] ahead of
///   time with a [`Component`] to front-load string lookups
///   of exports once instead of per-instantiation. This
///   method then uses [`RunnerPre::instantiate`] to
///   create a [`Runner`].
///
/// * If you've instantiated the instance yourself already
///   then you can use [`Runner::new`].
///
/// These methods are all equivalent to one another and move
/// around the tradeoff of what work is performed when.
///
/// [`Store`]: wasmtime::Store
/// [`Component`]: wasmtime::component::Component
/// [`Linker`]: wasmtime::component::Linker
pub struct Runner {
    interface0: exports::et::ws_wasi::entry::Guest,
}
const _: () = {
    impl RunnerIndices {
        /// Creates a new copy of `RunnerIndices` bindings which can then
        /// be used to instantiate into a particular store.
        ///
        /// This method may fail if the component does not have the
        /// required exports.
        pub fn new<_T>(
            _instance_pre: &wasmtime::component::InstancePre<_T>,
        ) -> wasmtime::Result<Self> {
            let _component = _instance_pre.component();
            let _instance_type = _instance_pre.instance_type();
            let interface0 = exports::et::ws_wasi::entry::GuestIndices::new(
                _instance_pre,
            )?;
            Ok(RunnerIndices { interface0 })
        }
        /// Uses the indices stored in `self` to load an instance
        /// of [`Runner`] from the instance provided.
        ///
        /// Note that at this time this method will additionally
        /// perform type-checks of all exports.
        pub fn load(
            &self,
            mut store: impl wasmtime::AsContextMut,
            instance: &wasmtime::component::Instance,
        ) -> wasmtime::Result<Runner> {
            let _ = &mut store;
            let _instance = instance;
            let interface0 = self.interface0.load(&mut store, &_instance)?;
            Ok(Runner { interface0 })
        }
    }
    impl Runner {
        /// Convenience wrapper around [`RunnerPre::new`] and
        /// [`RunnerPre::instantiate`].
        pub fn instantiate<_T>(
            store: impl wasmtime::AsContextMut<Data = _T>,
            component: &wasmtime::component::Component,
            linker: &wasmtime::component::Linker<_T>,
        ) -> wasmtime::Result<Runner> {
            let pre = linker.instantiate_pre(component)?;
            RunnerPre::new(pre)?.instantiate(store)
        }
        /// Convenience wrapper around [`RunnerIndices::new`] and
        /// [`RunnerIndices::load`].
        pub fn new(
            mut store: impl wasmtime::AsContextMut,
            instance: &wasmtime::component::Instance,
        ) -> wasmtime::Result<Runner> {
            let indices = RunnerIndices::new(&instance.instance_pre(&store))?;
            indices.load(&mut store, instance)
        }
        /// Convenience wrapper around [`RunnerPre::new`] and
        /// [`RunnerPre::instantiate_async`].
        pub async fn instantiate_async<_T>(
            store: impl wasmtime::AsContextMut<Data = _T>,
            component: &wasmtime::component::Component,
            linker: &wasmtime::component::Linker<_T>,
        ) -> wasmtime::Result<Runner>
        where
            _T: Send,
        {
            let pre = linker.instantiate_pre(component)?;
            RunnerPre::new(pre)?.instantiate_async(store).await
        }
        pub fn add_to_linker<T, D>(
            linker: &mut wasmtime::component::Linker<T>,
            host_getter: fn(&mut T) -> D::Data<'_>,
        ) -> wasmtime::Result<()>
        where
            D: wasi::logging::logging::HostWithStore<T>
                + wasi::keyvalue::store::HostWithStore<T>
                + et::ws_messages::messages::HostWithStore<T>
                + et::ws_wasi::ws::HostWithStore<T> + Send,
            for<'a> D::Data<
                'a,
            >: wasi::logging::logging::Host + wasi::keyvalue::store::Host
                + et::ws_messages::messages::Host + et::ws_wasi::ws::Host + Send,
            T: 'static + Send,
        {
            wasi::logging::logging::add_to_linker::<T, D>(linker, host_getter)?;
            wasi::keyvalue::store::add_to_linker::<T, D>(linker, host_getter)?;
            et::ws_messages::messages::add_to_linker::<T, D>(linker, host_getter)?;
            et::ws_wasi::ws::add_to_linker::<T, D>(linker, host_getter)?;
            Ok(())
        }
        pub fn et_ws_wasi_entry(&self) -> &exports::et::ws_wasi::entry::Guest {
            &self.interface0
        }
    }
};
pub mod et {
    pub mod ws_messages {
        #[allow(clippy::all)]
        pub mod messages {
            #[allow(unused_imports)]
            use wasmtime::component::__internal::Box;
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(enum)]
            #[derive(Clone, Copy, Eq, PartialEq)]
            #[repr(u8)]
            pub enum AgentConnectionState {
                #[component(name = "connected")]
                Connected,
                #[component(name = "disconnected")]
                Disconnected,
            }
            impl core::fmt::Debug for AgentConnectionState {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    match self {
                        AgentConnectionState::Connected => {
                            f.debug_tuple("AgentConnectionState::Connected").finish()
                        }
                        AgentConnectionState::Disconnected => {
                            f.debug_tuple("AgentConnectionState::Disconnected").finish()
                        }
                    }
                }
            }
            const _: () = {
                assert!(
                    1 == < AgentConnectionState as wasmtime::component::ComponentType
                    >::SIZE32
                );
                assert!(
                    1 == < AgentConnectionState as wasmtime::component::ComponentType
                    >::ALIGN32
                );
            };
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(enum)]
            #[derive(Clone, Copy, Eq, PartialEq)]
            #[repr(u8)]
            pub enum ConnectStatus {
                #[component(name = "assigned")]
                Assigned,
                #[component(name = "reconnected")]
                Reconnected,
            }
            impl core::fmt::Debug for ConnectStatus {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    match self {
                        ConnectStatus::Assigned => {
                            f.debug_tuple("ConnectStatus::Assigned").finish()
                        }
                        ConnectStatus::Reconnected => {
                            f.debug_tuple("ConnectStatus::Reconnected").finish()
                        }
                    }
                }
            }
            const _: () = {
                assert!(
                    1 == < ConnectStatus as wasmtime::component::ComponentType >::SIZE32
                );
                assert!(
                    1 == < ConnectStatus as wasmtime::component::ComponentType >::ALIGN32
                );
            };
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(enum)]
            #[derive(Clone, Copy, Eq, PartialEq)]
            #[repr(u8)]
            pub enum MessageDeliveryStatus {
                #[component(name = "delivered")]
                Delivered,
                #[component(name = "queued")]
                Queued,
                #[component(name = "acknowledged")]
                Acknowledged,
                #[component(name = "broadcast")]
                Broadcast,
            }
            impl core::fmt::Debug for MessageDeliveryStatus {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    match self {
                        MessageDeliveryStatus::Delivered => {
                            f.debug_tuple("MessageDeliveryStatus::Delivered").finish()
                        }
                        MessageDeliveryStatus::Queued => {
                            f.debug_tuple("MessageDeliveryStatus::Queued").finish()
                        }
                        MessageDeliveryStatus::Acknowledged => {
                            f.debug_tuple("MessageDeliveryStatus::Acknowledged").finish()
                        }
                        MessageDeliveryStatus::Broadcast => {
                            f.debug_tuple("MessageDeliveryStatus::Broadcast").finish()
                        }
                    }
                }
            }
            const _: () = {
                assert!(
                    1 == < MessageDeliveryStatus as wasmtime::component::ComponentType
                    >::SIZE32
                );
                assert!(
                    1 == < MessageDeliveryStatus as wasmtime::component::ComponentType
                    >::ALIGN32
                );
            };
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(enum)]
            #[derive(Clone, Copy, Eq, PartialEq)]
            #[repr(u8)]
            pub enum MessageScope {
                #[component(name = "direct")]
                Direct,
                #[component(name = "broadcast")]
                Broadcast,
            }
            impl core::fmt::Debug for MessageScope {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    match self {
                        MessageScope::Direct => {
                            f.debug_tuple("MessageScope::Direct").finish()
                        }
                        MessageScope::Broadcast => {
                            f.debug_tuple("MessageScope::Broadcast").finish()
                        }
                    }
                }
            }
            const _: () = {
                assert!(
                    1 == < MessageScope as wasmtime::component::ComponentType >::SIZE32
                );
                assert!(
                    1 == < MessageScope as wasmtime::component::ComponentType >::ALIGN32
                );
            };
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(record)]
            #[derive(Clone)]
            pub struct AgentSummary {
                #[component(name = "agent-id")]
                pub agent_id: wasmtime::component::__internal::String,
                #[component(name = "last-known-ip")]
                pub last_known_ip: Option<wasmtime::component::__internal::String>,
                #[component(name = "state")]
                pub state: AgentConnectionState,
            }
            impl core::fmt::Debug for AgentSummary {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    f.debug_struct("AgentSummary")
                        .field("agent-id", &self.agent_id)
                        .field("last-known-ip", &self.last_known_ip)
                        .field("state", &self.state)
                        .finish()
                }
            }
            const _: () = {
                assert!(
                    24 == < AgentSummary as wasmtime::component::ComponentType >::SIZE32
                );
                assert!(
                    4 == < AgentSummary as wasmtime::component::ComponentType >::ALIGN32
                );
            };
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(record)]
            #[derive(Clone)]
            pub struct ConnectPayload {
                #[component(name = "agent-id")]
                pub agent_id: Option<wasmtime::component::__internal::String>,
            }
            impl core::fmt::Debug for ConnectPayload {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    f.debug_struct("ConnectPayload")
                        .field("agent-id", &self.agent_id)
                        .finish()
                }
            }
            const _: () = {
                assert!(
                    12 == < ConnectPayload as wasmtime::component::ComponentType
                    >::SIZE32
                );
                assert!(
                    4 == < ConnectPayload as wasmtime::component::ComponentType
                    >::ALIGN32
                );
            };
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(record)]
            #[derive(Clone)]
            pub struct AlivePayload {
                #[component(name = "timestamp")]
                pub timestamp: wasmtime::component::__internal::String,
            }
            impl core::fmt::Debug for AlivePayload {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    f.debug_struct("AlivePayload")
                        .field("timestamp", &self.timestamp)
                        .finish()
                }
            }
            const _: () = {
                assert!(
                    8 == < AlivePayload as wasmtime::component::ComponentType >::SIZE32
                );
                assert!(
                    4 == < AlivePayload as wasmtime::component::ComponentType >::ALIGN32
                );
            };
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(record)]
            #[derive(Clone)]
            pub struct SendAgentMessagePayload {
                #[component(name = "message")]
                pub message: wasmtime::component::__internal::String,
                #[component(name = "to-agent-id")]
                pub to_agent_id: wasmtime::component::__internal::String,
            }
            impl core::fmt::Debug for SendAgentMessagePayload {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    f.debug_struct("SendAgentMessagePayload")
                        .field("message", &self.message)
                        .field("to-agent-id", &self.to_agent_id)
                        .finish()
                }
            }
            const _: () = {
                assert!(
                    16 == < SendAgentMessagePayload as wasmtime::component::ComponentType
                    >::SIZE32
                );
                assert!(
                    4 == < SendAgentMessagePayload as wasmtime::component::ComponentType
                    >::ALIGN32
                );
            };
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(record)]
            #[derive(Clone)]
            pub struct BroadcastMessagePayload {
                #[component(name = "message")]
                pub message: wasmtime::component::__internal::String,
            }
            impl core::fmt::Debug for BroadcastMessagePayload {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    f.debug_struct("BroadcastMessagePayload")
                        .field("message", &self.message)
                        .finish()
                }
            }
            const _: () = {
                assert!(
                    8 == < BroadcastMessagePayload as wasmtime::component::ComponentType
                    >::SIZE32
                );
                assert!(
                    4 == < BroadcastMessagePayload as wasmtime::component::ComponentType
                    >::ALIGN32
                );
            };
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(record)]
            #[derive(Clone)]
            pub struct MessageAckPayload {
                #[component(name = "message-id")]
                pub message_id: wasmtime::component::__internal::String,
            }
            impl core::fmt::Debug for MessageAckPayload {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    f.debug_struct("MessageAckPayload")
                        .field("message-id", &self.message_id)
                        .finish()
                }
            }
            const _: () = {
                assert!(
                    8 == < MessageAckPayload as wasmtime::component::ComponentType
                    >::SIZE32
                );
                assert!(
                    4 == < MessageAckPayload as wasmtime::component::ComponentType
                    >::ALIGN32
                );
            };
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(record)]
            #[derive(Clone)]
            pub struct ClientEventPayload {
                #[component(name = "action")]
                pub action: wasmtime::component::__internal::String,
                #[component(name = "capability")]
                pub capability: wasmtime::component::__internal::String,
                #[component(name = "details")]
                pub details: wasmtime::component::__internal::String,
            }
            impl core::fmt::Debug for ClientEventPayload {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    f.debug_struct("ClientEventPayload")
                        .field("action", &self.action)
                        .field("capability", &self.capability)
                        .field("details", &self.details)
                        .finish()
                }
            }
            const _: () = {
                assert!(
                    24 == < ClientEventPayload as wasmtime::component::ComponentType
                    >::SIZE32
                );
                assert!(
                    4 == < ClientEventPayload as wasmtime::component::ComponentType
                    >::ALIGN32
                );
            };
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(record)]
            #[derive(Clone)]
            pub struct RelayTextPayload {
                #[component(name = "content")]
                pub content: wasmtime::component::__internal::String,
            }
            impl core::fmt::Debug for RelayTextPayload {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    f.debug_struct("RelayTextPayload")
                        .field("content", &self.content)
                        .finish()
                }
            }
            const _: () = {
                assert!(
                    8 == < RelayTextPayload as wasmtime::component::ComponentType
                    >::SIZE32
                );
                assert!(
                    4 == < RelayTextPayload as wasmtime::component::ComponentType
                    >::ALIGN32
                );
            };
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(record)]
            #[derive(Clone)]
            pub struct RelayBinaryPayload {
                #[component(name = "content")]
                pub content: wasmtime::component::__internal::Vec<u8>,
            }
            impl core::fmt::Debug for RelayBinaryPayload {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    f.debug_struct("RelayBinaryPayload")
                        .field("content", &self.content)
                        .finish()
                }
            }
            const _: () = {
                assert!(
                    8 == < RelayBinaryPayload as wasmtime::component::ComponentType
                    >::SIZE32
                );
                assert!(
                    4 == < RelayBinaryPayload as wasmtime::component::ComponentType
                    >::ALIGN32
                );
            };
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(record)]
            #[derive(Clone)]
            pub struct ConnectAckPayload {
                #[component(name = "agent-id")]
                pub agent_id: wasmtime::component::__internal::String,
                #[component(name = "status")]
                pub status: ConnectStatus,
            }
            impl core::fmt::Debug for ConnectAckPayload {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    f.debug_struct("ConnectAckPayload")
                        .field("agent-id", &self.agent_id)
                        .field("status", &self.status)
                        .finish()
                }
            }
            const _: () = {
                assert!(
                    12 == < ConnectAckPayload as wasmtime::component::ComponentType
                    >::SIZE32
                );
                assert!(
                    4 == < ConnectAckPayload as wasmtime::component::ComponentType
                    >::ALIGN32
                );
            };
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(record)]
            #[derive(Clone)]
            pub struct ListAgentsResponsePayload {
                #[component(name = "agents")]
                pub agents: wasmtime::component::__internal::Vec<AgentSummary>,
            }
            impl core::fmt::Debug for ListAgentsResponsePayload {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    f.debug_struct("ListAgentsResponsePayload")
                        .field("agents", &self.agents)
                        .finish()
                }
            }
            const _: () = {
                assert!(
                    8 == < ListAgentsResponsePayload as
                    wasmtime::component::ComponentType >::SIZE32
                );
                assert!(
                    4 == < ListAgentsResponsePayload as
                    wasmtime::component::ComponentType >::ALIGN32
                );
            };
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(record)]
            #[derive(Clone)]
            pub struct AgentMessagePayload {
                #[component(name = "from-agent-id")]
                pub from_agent_id: wasmtime::component::__internal::String,
                #[component(name = "message")]
                pub message: wasmtime::component::__internal::String,
                #[component(name = "message-id")]
                pub message_id: wasmtime::component::__internal::String,
                #[component(name = "scope")]
                pub scope: MessageScope,
                #[component(name = "server-received-at")]
                pub server_received_at: wasmtime::component::__internal::String,
            }
            impl core::fmt::Debug for AgentMessagePayload {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    f.debug_struct("AgentMessagePayload")
                        .field("from-agent-id", &self.from_agent_id)
                        .field("message", &self.message)
                        .field("message-id", &self.message_id)
                        .field("scope", &self.scope)
                        .field("server-received-at", &self.server_received_at)
                        .finish()
                }
            }
            const _: () = {
                assert!(
                    36 == < AgentMessagePayload as wasmtime::component::ComponentType
                    >::SIZE32
                );
                assert!(
                    4 == < AgentMessagePayload as wasmtime::component::ComponentType
                    >::ALIGN32
                );
            };
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(record)]
            #[derive(Clone)]
            pub struct MessageStatusPayload {
                #[component(name = "detail")]
                pub detail: wasmtime::component::__internal::String,
                #[component(name = "message-id")]
                pub message_id: Option<wasmtime::component::__internal::String>,
                #[component(name = "status")]
                pub status: MessageDeliveryStatus,
            }
            impl core::fmt::Debug for MessageStatusPayload {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    f.debug_struct("MessageStatusPayload")
                        .field("detail", &self.detail)
                        .field("message-id", &self.message_id)
                        .field("status", &self.status)
                        .finish()
                }
            }
            const _: () = {
                assert!(
                    24 == < MessageStatusPayload as wasmtime::component::ComponentType
                    >::SIZE32
                );
                assert!(
                    4 == < MessageStatusPayload as wasmtime::component::ComponentType
                    >::ALIGN32
                );
            };
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(record)]
            #[derive(Clone)]
            pub struct InvalidPayload {
                #[component(name = "detail")]
                pub detail: wasmtime::component::__internal::String,
                #[component(name = "message-id")]
                pub message_id: Option<wasmtime::component::__internal::String>,
            }
            impl core::fmt::Debug for InvalidPayload {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    f.debug_struct("InvalidPayload")
                        .field("detail", &self.detail)
                        .field("message-id", &self.message_id)
                        .finish()
                }
            }
            const _: () = {
                assert!(
                    20 == < InvalidPayload as wasmtime::component::ComponentType
                    >::SIZE32
                );
                assert!(
                    4 == < InvalidPayload as wasmtime::component::ComponentType
                    >::ALIGN32
                );
            };
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(record)]
            #[derive(Clone)]
            pub struct ResponsePayload {
                #[component(name = "message")]
                pub message: wasmtime::component::__internal::String,
            }
            impl core::fmt::Debug for ResponsePayload {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    f.debug_struct("ResponsePayload")
                        .field("message", &self.message)
                        .finish()
                }
            }
            const _: () = {
                assert!(
                    8 == < ResponsePayload as wasmtime::component::ComponentType
                    >::SIZE32
                );
                assert!(
                    4 == < ResponsePayload as wasmtime::component::ComponentType
                    >::ALIGN32
                );
            };
            /// Messages a client is allowed to send (and the server receives).
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(variant)]
            #[derive(Clone)]
            pub enum ClientMessage {
                #[component(name = "connect")]
                Connect(ConnectPayload),
                #[component(name = "alive")]
                Alive(AlivePayload),
                #[component(name = "list-agents")]
                ListAgents,
                #[component(name = "send-agent-message")]
                SendAgentMessage(SendAgentMessagePayload),
                #[component(name = "broadcast-message")]
                BroadcastMessage(BroadcastMessagePayload),
                #[component(name = "message-ack")]
                MessageAck(MessageAckPayload),
                #[component(name = "client-event")]
                ClientEvent(ClientEventPayload),
                #[component(name = "relay-text")]
                RelayText(RelayTextPayload),
                #[component(name = "relay-binary")]
                RelayBinary(RelayBinaryPayload),
            }
            impl core::fmt::Debug for ClientMessage {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    match self {
                        ClientMessage::Connect(e) => {
                            f.debug_tuple("ClientMessage::Connect").field(e).finish()
                        }
                        ClientMessage::Alive(e) => {
                            f.debug_tuple("ClientMessage::Alive").field(e).finish()
                        }
                        ClientMessage::ListAgents => {
                            f.debug_tuple("ClientMessage::ListAgents").finish()
                        }
                        ClientMessage::SendAgentMessage(e) => {
                            f.debug_tuple("ClientMessage::SendAgentMessage")
                                .field(e)
                                .finish()
                        }
                        ClientMessage::BroadcastMessage(e) => {
                            f.debug_tuple("ClientMessage::BroadcastMessage")
                                .field(e)
                                .finish()
                        }
                        ClientMessage::MessageAck(e) => {
                            f.debug_tuple("ClientMessage::MessageAck").field(e).finish()
                        }
                        ClientMessage::ClientEvent(e) => {
                            f.debug_tuple("ClientMessage::ClientEvent").field(e).finish()
                        }
                        ClientMessage::RelayText(e) => {
                            f.debug_tuple("ClientMessage::RelayText").field(e).finish()
                        }
                        ClientMessage::RelayBinary(e) => {
                            f.debug_tuple("ClientMessage::RelayBinary").field(e).finish()
                        }
                    }
                }
            }
            const _: () = {
                assert!(
                    28 == < ClientMessage as wasmtime::component::ComponentType >::SIZE32
                );
                assert!(
                    4 == < ClientMessage as wasmtime::component::ComponentType >::ALIGN32
                );
            };
            /// Messages the server is allowed to send (and clients receive).
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(variant)]
            #[derive(Clone)]
            pub enum ServerMessage {
                #[component(name = "connect-ack")]
                ConnectAck(ConnectAckPayload),
                #[component(name = "list-agents-response")]
                ListAgentsResponse(ListAgentsResponsePayload),
                #[component(name = "agent-message")]
                AgentMessage(AgentMessagePayload),
                #[component(name = "message-status")]
                MessageStatus(MessageStatusPayload),
                #[component(name = "invalid")]
                Invalid(InvalidPayload),
                #[component(name = "response")]
                Response(ResponsePayload),
                #[component(name = "relay-text")]
                RelayText(RelayTextPayload),
                #[component(name = "relay-binary")]
                RelayBinary(RelayBinaryPayload),
            }
            impl core::fmt::Debug for ServerMessage {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    match self {
                        ServerMessage::ConnectAck(e) => {
                            f.debug_tuple("ServerMessage::ConnectAck").field(e).finish()
                        }
                        ServerMessage::ListAgentsResponse(e) => {
                            f.debug_tuple("ServerMessage::ListAgentsResponse")
                                .field(e)
                                .finish()
                        }
                        ServerMessage::AgentMessage(e) => {
                            f.debug_tuple("ServerMessage::AgentMessage")
                                .field(e)
                                .finish()
                        }
                        ServerMessage::MessageStatus(e) => {
                            f.debug_tuple("ServerMessage::MessageStatus")
                                .field(e)
                                .finish()
                        }
                        ServerMessage::Invalid(e) => {
                            f.debug_tuple("ServerMessage::Invalid").field(e).finish()
                        }
                        ServerMessage::Response(e) => {
                            f.debug_tuple("ServerMessage::Response").field(e).finish()
                        }
                        ServerMessage::RelayText(e) => {
                            f.debug_tuple("ServerMessage::RelayText").field(e).finish()
                        }
                        ServerMessage::RelayBinary(e) => {
                            f.debug_tuple("ServerMessage::RelayBinary").field(e).finish()
                        }
                    }
                }
            }
            const _: () = {
                assert!(
                    40 == < ServerMessage as wasmtime::component::ComponentType >::SIZE32
                );
                assert!(
                    4 == < ServerMessage as wasmtime::component::ComponentType >::ALIGN32
                );
            };
            pub trait HostWithStore<T>: wasmtime::component::HasData {}
            impl<H: ?Sized, T> HostWithStore<T> for H
            where
                H: wasmtime::component::HasData,
            {}
            pub trait Host {}
            impl<_T: Host + ?Sized> Host for &mut _T {}
            pub fn add_to_linker_instance<T, D>(
                inst: &mut wasmtime::component::LinkerInstance<'_, T>,
                host_getter: fn(&mut T) -> D::Data<'_>,
            ) -> wasmtime::Result<()>
            where
                D: HostWithStore<T>,
                for<'a> D::Data<'a>: Host,
                T: 'static,
            {
                Ok(())
            }
            pub fn add_to_linker<T, D>(
                linker: &mut wasmtime::component::Linker<T>,
                host_getter: fn(&mut T) -> D::Data<'_>,
            ) -> wasmtime::Result<()>
            where
                D: HostWithStore<T>,
                for<'a> D::Data<'a>: Host,
                T: 'static,
            {
                let mut inst = linker.instance("et:ws-messages/messages@0.1.0")?;
                add_to_linker_instance::<T, D>(&mut inst, host_getter)
            }
        }
    }
    pub mod ws_wasi {
        #[allow(clippy::all)]
        pub mod ws {
            #[allow(unused_imports)]
            use wasmtime::component::__internal::Box;
            pub type ClientMessage = super::super::super::et::ws_messages::messages::ClientMessage;
            const _: () = {
                assert!(
                    28 == < ClientMessage as wasmtime::component::ComponentType >::SIZE32
                );
                assert!(
                    4 == < ClientMessage as wasmtime::component::ComponentType >::ALIGN32
                );
            };
            pub type ServerMessage = super::super::super::et::ws_messages::messages::ServerMessage;
            const _: () = {
                assert!(
                    40 == < ServerMessage as wasmtime::component::ComponentType >::SIZE32
                );
                assert!(
                    4 == < ServerMessage as wasmtime::component::ComponentType >::ALIGN32
                );
            };
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(variant)]
            #[derive(Clone)]
            pub enum WsError {
                /// Connection-level failure: TCP/TLS, URL parse, HTTP upgrade, frame
                /// I/O, write-buffer-full, and tungstenite's wire-protocol violations
                /// (frame format, capacity, attack-attempt). Anything that means the
                /// bytes never went through cleanly.
                #[component(name = "transport")]
                Transport(wasmtime::component::__internal::String),
                /// Inbound JSON's `type` field starts with `et-` (claiming to be one
                /// of our messages) but the rest of the payload doesn't match the
                /// declared variant. Raised ONLY for that case: any other inbound
                /// parse failure (non-JSON, JSON without a `type`, JSON with a
                /// non-et-prefixed `type`) is relayed instead -- the ws protocol
                /// forwards binary and unrecognised frames unchanged. Also raised
                /// for host-side serialize failures of outbound messages.
                #[component(name = "decode")]
                Decode(wasmtime::component::__internal::String),
                #[component(name = "not-connected")]
                NotConnected,
                #[component(name = "already-connected")]
                AlreadyConnected,
            }
            impl core::fmt::Debug for WsError {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    match self {
                        WsError::Transport(e) => {
                            f.debug_tuple("WsError::Transport").field(e).finish()
                        }
                        WsError::Decode(e) => {
                            f.debug_tuple("WsError::Decode").field(e).finish()
                        }
                        WsError::NotConnected => {
                            f.debug_tuple("WsError::NotConnected").finish()
                        }
                        WsError::AlreadyConnected => {
                            f.debug_tuple("WsError::AlreadyConnected").finish()
                        }
                    }
                }
            }
            impl core::fmt::Display for WsError {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    write!(f, "{:?}", self)
                }
            }
            impl core::error::Error for WsError {}
            const _: () = {
                assert!(12 == < WsError as wasmtime::component::ComponentType >::SIZE32);
                assert!(4 == < WsError as wasmtime::component::ComponentType >::ALIGN32);
            };
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(enum)]
            #[derive(Clone, Copy, Eq, PartialEq)]
            #[repr(u8)]
            pub enum State {
                #[component(name = "connecting")]
                Connecting,
                #[component(name = "connected")]
                Connected,
                #[component(name = "closing")]
                Closing,
                #[component(name = "closed")]
                Closed,
            }
            impl core::fmt::Debug for State {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    match self {
                        State::Connecting => f.debug_tuple("State::Connecting").finish(),
                        State::Connected => f.debug_tuple("State::Connected").finish(),
                        State::Closing => f.debug_tuple("State::Closing").finish(),
                        State::Closed => f.debug_tuple("State::Closed").finish(),
                    }
                }
            }
            const _: () = {
                assert!(1 == < State as wasmtime::component::ComponentType >::SIZE32);
                assert!(1 == < State as wasmtime::component::ComponentType >::ALIGN32);
            };
            pub trait HostWithStore<T>: wasmtime::component::HasData + Send {}
            impl<H: ?Sized, T> HostWithStore<T> for H
            where
                H: wasmtime::component::HasData + Send,
            {}
            pub trait Host: Send {
                /// `send` accepts only what a client is allowed to emit; `recv`
                /// returns only what the server is allowed to send. Foreign frames
                /// (binary or text whose `type` isn't `et-*`) are surfaced through
                /// the `et-relay-text` / `et-relay-binary` variants present in both
                /// directions, so the typed catalog covers both our schema and the
                /// hub-relay passthrough.
                fn connect(
                    &mut self,
                ) -> impl ::core::future::Future<Output = Result<(), WsError>> + Send;
                fn get_state(
                    &mut self,
                ) -> impl ::core::future::Future<Output = State> + Send;
                fn agent_id(
                    &mut self,
                ) -> impl ::core::future::Future<
                    Output = wasmtime::component::__internal::String,
                > + Send;
                fn send(
                    &mut self,
                    message: ClientMessage,
                ) -> impl ::core::future::Future<Output = Result<(), WsError>> + Send;
                fn recv(
                    &mut self,
                    timeout_ms: u32,
                ) -> impl ::core::future::Future<
                    Output = Result<Option<ServerMessage>, WsError>,
                > + Send;
                fn disconnect(
                    &mut self,
                ) -> impl ::core::future::Future<Output = ()> + Send;
            }
            impl<_T: Host + ?Sized + Send> Host for &mut _T {
                /// `send` accepts only what a client is allowed to emit; `recv`
                /// returns only what the server is allowed to send. Foreign frames
                /// (binary or text whose `type` isn't `et-*`) are surfaced through
                /// the `et-relay-text` / `et-relay-binary` variants present in both
                /// directions, so the typed catalog covers both our schema and the
                /// hub-relay passthrough.
                fn connect(
                    &mut self,
                ) -> impl ::core::future::Future<Output = Result<(), WsError>> + Send {
                    async move { Host::connect(*self).await }
                }
                fn get_state(
                    &mut self,
                ) -> impl ::core::future::Future<Output = State> + Send {
                    async move { Host::get_state(*self).await }
                }
                fn agent_id(
                    &mut self,
                ) -> impl ::core::future::Future<
                    Output = wasmtime::component::__internal::String,
                > + Send {
                    async move { Host::agent_id(*self).await }
                }
                fn send(
                    &mut self,
                    message: ClientMessage,
                ) -> impl ::core::future::Future<Output = Result<(), WsError>> + Send {
                    async move { Host::send(*self, message).await }
                }
                fn recv(
                    &mut self,
                    timeout_ms: u32,
                ) -> impl ::core::future::Future<
                    Output = Result<Option<ServerMessage>, WsError>,
                > + Send {
                    async move { Host::recv(*self, timeout_ms).await }
                }
                fn disconnect(
                    &mut self,
                ) -> impl ::core::future::Future<Output = ()> + Send {
                    async move { Host::disconnect(*self).await }
                }
            }
            pub fn add_to_linker_instance<T, D>(
                inst: &mut wasmtime::component::LinkerInstance<'_, T>,
                host_getter: fn(&mut T) -> D::Data<'_>,
            ) -> wasmtime::Result<()>
            where
                D: HostWithStore<T>,
                for<'a> D::Data<'a>: Host,
                T: 'static + Send,
            {
                inst.func_wrap_async(
                    "connect",
                    move |mut caller: wasmtime::StoreContextMut<'_, T>, (): ()| {
                        wasmtime::component::__internal::Box::new(async move {
                            let host = &mut host_getter(caller.data_mut());
                            let r = Host::connect(host).await;
                            Ok((r,))
                        })
                    },
                )?;
                inst.func_wrap_async(
                    "get-state",
                    move |mut caller: wasmtime::StoreContextMut<'_, T>, (): ()| {
                        wasmtime::component::__internal::Box::new(async move {
                            let host = &mut host_getter(caller.data_mut());
                            let r = Host::get_state(host).await;
                            Ok((r,))
                        })
                    },
                )?;
                inst.func_wrap_async(
                    "agent-id",
                    move |mut caller: wasmtime::StoreContextMut<'_, T>, (): ()| {
                        wasmtime::component::__internal::Box::new(async move {
                            let host = &mut host_getter(caller.data_mut());
                            let r = Host::agent_id(host).await;
                            Ok((r,))
                        })
                    },
                )?;
                inst.func_wrap_async(
                    "send",
                    move |
                        mut caller: wasmtime::StoreContextMut<'_, T>,
                        (arg0,): (ClientMessage,)|
                    {
                        wasmtime::component::__internal::Box::new(async move {
                            let host = &mut host_getter(caller.data_mut());
                            let r = Host::send(host, arg0).await;
                            Ok((r,))
                        })
                    },
                )?;
                inst.func_wrap_async(
                    "recv",
                    move |mut caller: wasmtime::StoreContextMut<'_, T>, (arg0,): (u32,)| {
                        wasmtime::component::__internal::Box::new(async move {
                            let host = &mut host_getter(caller.data_mut());
                            let r = Host::recv(host, arg0).await;
                            Ok((r,))
                        })
                    },
                )?;
                inst.func_wrap_async(
                    "disconnect",
                    move |mut caller: wasmtime::StoreContextMut<'_, T>, (): ()| {
                        wasmtime::component::__internal::Box::new(async move {
                            let host = &mut host_getter(caller.data_mut());
                            let r = Host::disconnect(host).await;
                            Ok(r)
                        })
                    },
                )?;
                Ok(())
            }
            pub fn add_to_linker<T, D>(
                linker: &mut wasmtime::component::Linker<T>,
                host_getter: fn(&mut T) -> D::Data<'_>,
            ) -> wasmtime::Result<()>
            where
                D: HostWithStore<T>,
                for<'a> D::Data<'a>: Host,
                T: 'static + Send,
            {
                let mut inst = linker.instance("et:ws-wasi/ws@0.1.0")?;
                add_to_linker_instance::<T, D>(&mut inst, host_getter)
            }
        }
    }
}
pub mod wasi {
    pub mod keyvalue {
        #[allow(clippy::all)]
        pub mod store {
            #[allow(unused_imports)]
            use wasmtime::component::__internal::Box;
            /// The set of errors which may be raised by functions in this package
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(variant)]
            #[derive(Clone)]
            pub enum Error {
                /// The host does not recognize the store identifier requested.
                #[component(name = "no-such-store")]
                NoSuchStore,
                /// The requesting component does not have access to the specified store
                /// (which may or may not exist).
                #[component(name = "access-denied")]
                AccessDenied,
                /// Some implementation-specific error has occurred (e.g. I/O)
                #[component(name = "other")]
                Other(wasmtime::component::__internal::String),
            }
            impl core::fmt::Debug for Error {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    match self {
                        Error::NoSuchStore => {
                            f.debug_tuple("Error::NoSuchStore").finish()
                        }
                        Error::AccessDenied => {
                            f.debug_tuple("Error::AccessDenied").finish()
                        }
                        Error::Other(e) => {
                            f.debug_tuple("Error::Other").field(e).finish()
                        }
                    }
                }
            }
            impl core::fmt::Display for Error {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    write!(f, "{:?}", self)
                }
            }
            impl core::error::Error for Error {}
            const _: () = {
                assert!(12 == < Error as wasmtime::component::ComponentType >::SIZE32);
                assert!(4 == < Error as wasmtime::component::ComponentType >::ALIGN32);
            };
            /// A response to a `list-keys` operation.
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(record)]
            #[derive(Clone)]
            pub struct KeyResponse {
                /// The list of keys returned by the query.
                #[component(name = "keys")]
                pub keys: wasmtime::component::__internal::Vec<
                    wasmtime::component::__internal::String,
                >,
                /// The continuation token to use to fetch the next page of keys. If this is `null`, then
                /// there are no more keys to fetch.
                #[component(name = "cursor")]
                pub cursor: Option<wasmtime::component::__internal::String>,
            }
            impl core::fmt::Debug for KeyResponse {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    f.debug_struct("KeyResponse")
                        .field("keys", &self.keys)
                        .field("cursor", &self.cursor)
                        .finish()
                }
            }
            const _: () = {
                assert!(
                    20 == < KeyResponse as wasmtime::component::ComponentType >::SIZE32
                );
                assert!(
                    4 == < KeyResponse as wasmtime::component::ComponentType >::ALIGN32
                );
            };
            /// A bucket is a collection of key-value pairs. Each key-value pair is stored as a entry in the
            /// bucket, and the bucket itself acts as a collection of all these entries.
            ///
            /// It is worth noting that the exact terminology for bucket in key-value stores can very
            /// depending on the specific implementation. For example:
            ///
            /// 1. Amazon DynamoDB calls a collection of key-value pairs a table
            /// 2. Redis has hashes, sets, and sorted sets as different types of collections
            /// 3. Cassandra calls a collection of key-value pairs a column family
            /// 4. MongoDB calls a collection of key-value pairs a collection
            /// 5. Riak calls a collection of key-value pairs a bucket
            /// 6. Memcached calls a collection of key-value pairs a slab
            /// 7. Azure Cosmos DB calls a collection of key-value pairs a container
            ///
            /// In this interface, we use the term `bucket` to refer to a collection of key-value pairs
            pub use super::super::super::__with_name0 as Bucket;
            pub trait HostBucketWithStore<T>: wasmtime::component::HasData + Send {}
            impl<H: ?Sized, T> HostBucketWithStore<T> for H
            where
                H: wasmtime::component::HasData + Send,
            {}
            pub trait HostBucket: Send {
                /// Get the value associated with the specified `key`
                ///
                /// The value is returned as an option. If the key-value pair exists in the
                /// store, it returns `Ok(value)`. If the key does not exist in the
                /// store, it returns `Ok(none)`.
                ///
                /// If any other error occurs, it returns an `Err(error)`.
                fn get(
                    &mut self,
                    self_: wasmtime::component::Resource<Bucket>,
                    key: wasmtime::component::__internal::String,
                ) -> impl ::core::future::Future<
                    Output = Result<
                        Option<wasmtime::component::__internal::Vec<u8>>,
                        Error,
                    >,
                > + Send;
                /// Set the value associated with the key in the store. If the key already
                /// exists in the store, it overwrites the value.
                ///
                /// If the key does not exist in the store, it creates a new key-value pair.
                ///
                /// If any other error occurs, it returns an `Err(error)`.
                fn set(
                    &mut self,
                    self_: wasmtime::component::Resource<Bucket>,
                    key: wasmtime::component::__internal::String,
                    value: wasmtime::component::__internal::Vec<u8>,
                ) -> impl ::core::future::Future<Output = Result<(), Error>> + Send;
                /// Delete the key-value pair associated with the key in the store.
                ///
                /// If the key does not exist in the store, it does nothing.
                ///
                /// If any other error occurs, it returns an `Err(error)`.
                fn delete(
                    &mut self,
                    self_: wasmtime::component::Resource<Bucket>,
                    key: wasmtime::component::__internal::String,
                ) -> impl ::core::future::Future<Output = Result<(), Error>> + Send;
                /// Check if the key exists in the store.
                ///
                /// If the key exists in the store, it returns `Ok(true)`. If the key does
                /// not exist in the store, it returns `Ok(false)`.
                ///
                /// If any other error occurs, it returns an `Err(error)`.
                fn exists(
                    &mut self,
                    self_: wasmtime::component::Resource<Bucket>,
                    key: wasmtime::component::__internal::String,
                ) -> impl ::core::future::Future<Output = Result<bool, Error>> + Send;
                /// Get all the keys in the store with an optional cursor (for use in pagination). It
                /// returns a list of keys. Please note that for most KeyValue implementations, this is a
                /// can be a very expensive operation and so it should be used judiciously. Implementations
                /// can return any number of keys in a single response, but they should never attempt to
                /// send more data than is reasonable (i.e. on a small edge device, this may only be a few
                /// KB, while on a large machine this could be several MB). Any response should also return
                /// a cursor that can be used to fetch the next page of keys. See the `key-response` record
                /// for more information.
                ///
                /// Note that the keys are not guaranteed to be returned in any particular order.
                ///
                /// If the store is empty, it returns an empty list.
                ///
                /// MAY show an out-of-date list of keys if there are concurrent writes to the store.
                ///
                /// If any error occurs, it returns an `Err(error)`.
                fn list_keys(
                    &mut self,
                    self_: wasmtime::component::Resource<Bucket>,
                    cursor: Option<wasmtime::component::__internal::String>,
                ) -> impl ::core::future::Future<
                    Output = Result<KeyResponse, Error>,
                > + Send;
                fn drop(
                    &mut self,
                    rep: wasmtime::component::Resource<Bucket>,
                ) -> impl ::core::future::Future<Output = wasmtime::Result<()>> + Send;
            }
            impl<_T: HostBucket + ?Sized + Send> HostBucket for &mut _T {
                /// Get the value associated with the specified `key`
                ///
                /// The value is returned as an option. If the key-value pair exists in the
                /// store, it returns `Ok(value)`. If the key does not exist in the
                /// store, it returns `Ok(none)`.
                ///
                /// If any other error occurs, it returns an `Err(error)`.
                fn get(
                    &mut self,
                    self_: wasmtime::component::Resource<Bucket>,
                    key: wasmtime::component::__internal::String,
                ) -> impl ::core::future::Future<
                    Output = Result<
                        Option<wasmtime::component::__internal::Vec<u8>>,
                        Error,
                    >,
                > + Send {
                    async move { HostBucket::get(*self, self_, key).await }
                }
                /// Set the value associated with the key in the store. If the key already
                /// exists in the store, it overwrites the value.
                ///
                /// If the key does not exist in the store, it creates a new key-value pair.
                ///
                /// If any other error occurs, it returns an `Err(error)`.
                fn set(
                    &mut self,
                    self_: wasmtime::component::Resource<Bucket>,
                    key: wasmtime::component::__internal::String,
                    value: wasmtime::component::__internal::Vec<u8>,
                ) -> impl ::core::future::Future<Output = Result<(), Error>> + Send {
                    async move { HostBucket::set(*self, self_, key, value).await }
                }
                /// Delete the key-value pair associated with the key in the store.
                ///
                /// If the key does not exist in the store, it does nothing.
                ///
                /// If any other error occurs, it returns an `Err(error)`.
                fn delete(
                    &mut self,
                    self_: wasmtime::component::Resource<Bucket>,
                    key: wasmtime::component::__internal::String,
                ) -> impl ::core::future::Future<Output = Result<(), Error>> + Send {
                    async move { HostBucket::delete(*self, self_, key).await }
                }
                /// Check if the key exists in the store.
                ///
                /// If the key exists in the store, it returns `Ok(true)`. If the key does
                /// not exist in the store, it returns `Ok(false)`.
                ///
                /// If any other error occurs, it returns an `Err(error)`.
                fn exists(
                    &mut self,
                    self_: wasmtime::component::Resource<Bucket>,
                    key: wasmtime::component::__internal::String,
                ) -> impl ::core::future::Future<Output = Result<bool, Error>> + Send {
                    async move { HostBucket::exists(*self, self_, key).await }
                }
                /// Get all the keys in the store with an optional cursor (for use in pagination). It
                /// returns a list of keys. Please note that for most KeyValue implementations, this is a
                /// can be a very expensive operation and so it should be used judiciously. Implementations
                /// can return any number of keys in a single response, but they should never attempt to
                /// send more data than is reasonable (i.e. on a small edge device, this may only be a few
                /// KB, while on a large machine this could be several MB). Any response should also return
                /// a cursor that can be used to fetch the next page of keys. See the `key-response` record
                /// for more information.
                ///
                /// Note that the keys are not guaranteed to be returned in any particular order.
                ///
                /// If the store is empty, it returns an empty list.
                ///
                /// MAY show an out-of-date list of keys if there are concurrent writes to the store.
                ///
                /// If any error occurs, it returns an `Err(error)`.
                fn list_keys(
                    &mut self,
                    self_: wasmtime::component::Resource<Bucket>,
                    cursor: Option<wasmtime::component::__internal::String>,
                ) -> impl ::core::future::Future<
                    Output = Result<KeyResponse, Error>,
                > + Send {
                    async move { HostBucket::list_keys(*self, self_, cursor).await }
                }
                async fn drop(
                    &mut self,
                    rep: wasmtime::component::Resource<Bucket>,
                ) -> wasmtime::Result<()> {
                    HostBucket::drop(*self, rep).await
                }
            }
            pub trait HostWithStore<
                T,
            >: wasmtime::component::HasData + HostBucketWithStore<T> + Send {}
            impl<H: ?Sized, T> HostWithStore<T> for H
            where
                H: wasmtime::component::HasData + HostBucketWithStore<T> + Send,
            {}
            pub trait Host: HostBucket + Send {
                /// Get the bucket with the specified identifier.
                ///
                /// `identifier` must refer to a bucket provided by the host.
                ///
                /// `error::no-such-store` will be raised if the `identifier` is not recognized.
                fn open(
                    &mut self,
                    identifier: wasmtime::component::__internal::String,
                ) -> impl ::core::future::Future<
                    Output = Result<wasmtime::component::Resource<Bucket>, Error>,
                > + Send;
            }
            impl<_T: Host + ?Sized + Send> Host for &mut _T {
                /// Get the bucket with the specified identifier.
                ///
                /// `identifier` must refer to a bucket provided by the host.
                ///
                /// `error::no-such-store` will be raised if the `identifier` is not recognized.
                fn open(
                    &mut self,
                    identifier: wasmtime::component::__internal::String,
                ) -> impl ::core::future::Future<
                    Output = Result<wasmtime::component::Resource<Bucket>, Error>,
                > + Send {
                    async move { Host::open(*self, identifier).await }
                }
            }
            pub fn add_to_linker_instance<T, D>(
                inst: &mut wasmtime::component::LinkerInstance<'_, T>,
                host_getter: fn(&mut T) -> D::Data<'_>,
            ) -> wasmtime::Result<()>
            where
                D: HostWithStore<T>,
                for<'a> D::Data<'a>: Host,
                T: 'static + Send,
            {
                inst.resource_async(
                    "bucket",
                    wasmtime::component::ResourceType::host::<Bucket>(),
                    move |mut store, rep| {
                        wasmtime::component::__internal::Box::new(async move {
                            wasmtime::ToWasmtimeResult::to_wasmtime_result(
                                HostBucket::drop(
                                        &mut host_getter(store.data_mut()),
                                        wasmtime::component::Resource::new_own(rep),
                                    )
                                    .await,
                            )
                        })
                    },
                )?;
                inst.func_wrap_async(
                    "open",
                    move |
                        mut caller: wasmtime::StoreContextMut<'_, T>,
                        (arg0,): (wasmtime::component::__internal::String,)|
                    {
                        wasmtime::component::__internal::Box::new(async move {
                            let host = &mut host_getter(caller.data_mut());
                            let r = Host::open(host, arg0).await;
                            Ok((r,))
                        })
                    },
                )?;
                inst.func_wrap_async(
                    "[method]bucket.get",
                    move |
                        mut caller: wasmtime::StoreContextMut<'_, T>,
                        (
                            arg0,
                            arg1,
                        ): (
                            wasmtime::component::Resource<Bucket>,
                            wasmtime::component::__internal::String,
                        )|
                    {
                        wasmtime::component::__internal::Box::new(async move {
                            let host = &mut host_getter(caller.data_mut());
                            let r = HostBucket::get(host, arg0, arg1).await;
                            Ok((r,))
                        })
                    },
                )?;
                inst.func_wrap_async(
                    "[method]bucket.set",
                    move |
                        mut caller: wasmtime::StoreContextMut<'_, T>,
                        (
                            arg0,
                            arg1,
                            arg2,
                        ): (
                            wasmtime::component::Resource<Bucket>,
                            wasmtime::component::__internal::String,
                            wasmtime::component::__internal::Vec<u8>,
                        )|
                    {
                        wasmtime::component::__internal::Box::new(async move {
                            let host = &mut host_getter(caller.data_mut());
                            let r = HostBucket::set(host, arg0, arg1, arg2).await;
                            Ok((r,))
                        })
                    },
                )?;
                inst.func_wrap_async(
                    "[method]bucket.delete",
                    move |
                        mut caller: wasmtime::StoreContextMut<'_, T>,
                        (
                            arg0,
                            arg1,
                        ): (
                            wasmtime::component::Resource<Bucket>,
                            wasmtime::component::__internal::String,
                        )|
                    {
                        wasmtime::component::__internal::Box::new(async move {
                            let host = &mut host_getter(caller.data_mut());
                            let r = HostBucket::delete(host, arg0, arg1).await;
                            Ok((r,))
                        })
                    },
                )?;
                inst.func_wrap_async(
                    "[method]bucket.exists",
                    move |
                        mut caller: wasmtime::StoreContextMut<'_, T>,
                        (
                            arg0,
                            arg1,
                        ): (
                            wasmtime::component::Resource<Bucket>,
                            wasmtime::component::__internal::String,
                        )|
                    {
                        wasmtime::component::__internal::Box::new(async move {
                            let host = &mut host_getter(caller.data_mut());
                            let r = HostBucket::exists(host, arg0, arg1).await;
                            Ok((r,))
                        })
                    },
                )?;
                inst.func_wrap_async(
                    "[method]bucket.list-keys",
                    move |
                        mut caller: wasmtime::StoreContextMut<'_, T>,
                        (
                            arg0,
                            arg1,
                        ): (
                            wasmtime::component::Resource<Bucket>,
                            Option<wasmtime::component::__internal::String>,
                        )|
                    {
                        wasmtime::component::__internal::Box::new(async move {
                            let host = &mut host_getter(caller.data_mut());
                            let r = HostBucket::list_keys(host, arg0, arg1).await;
                            Ok((r,))
                        })
                    },
                )?;
                Ok(())
            }
            pub fn add_to_linker<T, D>(
                linker: &mut wasmtime::component::Linker<T>,
                host_getter: fn(&mut T) -> D::Data<'_>,
            ) -> wasmtime::Result<()>
            where
                D: HostWithStore<T>,
                for<'a> D::Data<'a>: Host,
                T: 'static + Send,
            {
                let mut inst = linker.instance("wasi:keyvalue/store@0.2.0-draft")?;
                add_to_linker_instance::<T, D>(&mut inst, host_getter)
            }
        }
    }
    pub mod logging {
        #[allow(clippy::all)]
        pub mod logging {
            #[allow(unused_imports)]
            use wasmtime::component::__internal::Box;
            /// A log level, describing a kind of message.
            #[derive(wasmtime::component::ComponentType)]
            #[derive(wasmtime::component::Lift)]
            #[derive(wasmtime::component::Lower)]
            #[component(enum)]
            #[derive(Clone, Copy, Eq, PartialEq)]
            #[repr(u8)]
            pub enum Level {
                /// Describes messages about the values of variables and the flow of
                /// control within a program.
                #[component(name = "trace")]
                Trace,
                /// Describes messages likely to be of interest to someone debugging a
                /// program.
                #[component(name = "debug")]
                Debug,
                /// Describes messages likely to be of interest to someone monitoring a
                /// program.
                #[component(name = "info")]
                Info,
                /// Describes messages indicating hazardous situations.
                #[component(name = "warn")]
                Warn,
                /// Describes messages indicating serious errors.
                #[component(name = "error")]
                Error,
                /// Describes messages indicating fatal errors.
                #[component(name = "critical")]
                Critical,
            }
            impl core::fmt::Debug for Level {
                fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
                    match self {
                        Level::Trace => f.debug_tuple("Level::Trace").finish(),
                        Level::Debug => f.debug_tuple("Level::Debug").finish(),
                        Level::Info => f.debug_tuple("Level::Info").finish(),
                        Level::Warn => f.debug_tuple("Level::Warn").finish(),
                        Level::Error => f.debug_tuple("Level::Error").finish(),
                        Level::Critical => f.debug_tuple("Level::Critical").finish(),
                    }
                }
            }
            const _: () = {
                assert!(1 == < Level as wasmtime::component::ComponentType >::SIZE32);
                assert!(1 == < Level as wasmtime::component::ComponentType >::ALIGN32);
            };
            pub trait HostWithStore<T>: wasmtime::component::HasData + Send {}
            impl<H: ?Sized, T> HostWithStore<T> for H
            where
                H: wasmtime::component::HasData + Send,
            {}
            pub trait Host: Send {
                /// Emit a log message.
                ///
                /// A log message has a `level` describing what kind of message is being
                /// sent, a context, which is an uninterpreted string meant to help
                /// consumers group similar messages, and a string containing the message
                /// text.
                fn log(
                    &mut self,
                    level: Level,
                    context: wasmtime::component::__internal::String,
                    message: wasmtime::component::__internal::String,
                ) -> impl ::core::future::Future<Output = ()> + Send;
            }
            impl<_T: Host + ?Sized + Send> Host for &mut _T {
                /// Emit a log message.
                ///
                /// A log message has a `level` describing what kind of message is being
                /// sent, a context, which is an uninterpreted string meant to help
                /// consumers group similar messages, and a string containing the message
                /// text.
                fn log(
                    &mut self,
                    level: Level,
                    context: wasmtime::component::__internal::String,
                    message: wasmtime::component::__internal::String,
                ) -> impl ::core::future::Future<Output = ()> + Send {
                    async move { Host::log(*self, level, context, message).await }
                }
            }
            pub fn add_to_linker_instance<T, D>(
                inst: &mut wasmtime::component::LinkerInstance<'_, T>,
                host_getter: fn(&mut T) -> D::Data<'_>,
            ) -> wasmtime::Result<()>
            where
                D: HostWithStore<T>,
                for<'a> D::Data<'a>: Host,
                T: 'static + Send,
            {
                inst.func_wrap_async(
                    "log",
                    move |
                        mut caller: wasmtime::StoreContextMut<'_, T>,
                        (
                            arg0,
                            arg1,
                            arg2,
                        ): (
                            Level,
                            wasmtime::component::__internal::String,
                            wasmtime::component::__internal::String,
                        )|
                    {
                        wasmtime::component::__internal::Box::new(async move {
                            let host = &mut host_getter(caller.data_mut());
                            let r = Host::log(host, arg0, arg1, arg2).await;
                            Ok(r)
                        })
                    },
                )?;
                Ok(())
            }
            pub fn add_to_linker<T, D>(
                linker: &mut wasmtime::component::Linker<T>,
                host_getter: fn(&mut T) -> D::Data<'_>,
            ) -> wasmtime::Result<()>
            where
                D: HostWithStore<T>,
                for<'a> D::Data<'a>: Host,
                T: 'static + Send,
            {
                let mut inst = linker.instance("wasi:logging/logging@0.1.0-draft")?;
                add_to_linker_instance::<T, D>(&mut inst, host_getter)
            }
        }
    }
}
pub mod exports {
    pub mod et {
        pub mod ws_wasi {
            #[allow(clippy::all)]
            pub mod entry {
                #[allow(unused_imports)]
                use wasmtime::component::__internal::Box;
                pub type WsError = super::super::super::super::et::ws_wasi::ws::WsError;
                const _: () = {
                    assert!(
                        12 == < WsError as wasmtime::component::ComponentType >::SIZE32
                    );
                    assert!(
                        4 == < WsError as wasmtime::component::ComponentType >::ALIGN32
                    );
                };
                pub type StoreError = super::super::super::super::wasi::keyvalue::store::Error;
                const _: () = {
                    assert!(
                        12 == < StoreError as wasmtime::component::ComponentType
                        >::SIZE32
                    );
                    assert!(
                        4 == < StoreError as wasmtime::component::ComponentType
                        >::ALIGN32
                    );
                };
                /// Mirrors `wasi:nn/errors.error-code`. Kept local so guests that don't
                /// import wasi:nn don't gain a transitive dependency on it.
                #[derive(wasmtime::component::ComponentType)]
                #[derive(wasmtime::component::Lift)]
                #[derive(wasmtime::component::Lower)]
                #[component(enum)]
                #[derive(Clone, Copy, Eq, PartialEq)]
                #[repr(u8)]
                pub enum NnErrorCode {
                    #[component(name = "invalid-argument")]
                    InvalidArgument,
                    #[component(name = "invalid-encoding")]
                    InvalidEncoding,
                    #[component(name = "timeout")]
                    Timeout,
                    #[component(name = "runtime-error")]
                    RuntimeError,
                    #[component(name = "unsupported-operation")]
                    UnsupportedOperation,
                    #[component(name = "too-large")]
                    TooLarge,
                    #[component(name = "not-found")]
                    NotFound,
                    #[component(name = "security")]
                    Security,
                    #[component(name = "unknown")]
                    Unknown,
                }
                impl core::fmt::Debug for NnErrorCode {
                    fn fmt(
                        &self,
                        f: &mut core::fmt::Formatter<'_>,
                    ) -> core::fmt::Result {
                        match self {
                            NnErrorCode::InvalidArgument => {
                                f.debug_tuple("NnErrorCode::InvalidArgument").finish()
                            }
                            NnErrorCode::InvalidEncoding => {
                                f.debug_tuple("NnErrorCode::InvalidEncoding").finish()
                            }
                            NnErrorCode::Timeout => {
                                f.debug_tuple("NnErrorCode::Timeout").finish()
                            }
                            NnErrorCode::RuntimeError => {
                                f.debug_tuple("NnErrorCode::RuntimeError").finish()
                            }
                            NnErrorCode::UnsupportedOperation => {
                                f.debug_tuple("NnErrorCode::UnsupportedOperation").finish()
                            }
                            NnErrorCode::TooLarge => {
                                f.debug_tuple("NnErrorCode::TooLarge").finish()
                            }
                            NnErrorCode::NotFound => {
                                f.debug_tuple("NnErrorCode::NotFound").finish()
                            }
                            NnErrorCode::Security => {
                                f.debug_tuple("NnErrorCode::Security").finish()
                            }
                            NnErrorCode::Unknown => {
                                f.debug_tuple("NnErrorCode::Unknown").finish()
                            }
                        }
                    }
                }
                const _: () = {
                    assert!(
                        1 == < NnErrorCode as wasmtime::component::ComponentType
                        >::SIZE32
                    );
                    assert!(
                        1 == < NnErrorCode as wasmtime::component::ComponentType
                        >::ALIGN32
                    );
                };
                /// Flattened `wasi:nn/errors.error`. The upstream type is a resource
                /// (handle into the wasi-nn host), and variant payloads in WIT must be
                /// values, so guests destructure the resource at the catch site:
                ///   code = err.code(); data = err.data();
                #[derive(wasmtime::component::ComponentType)]
                #[derive(wasmtime::component::Lift)]
                #[derive(wasmtime::component::Lower)]
                #[component(record)]
                #[derive(Clone)]
                pub struct NnError {
                    #[component(name = "code")]
                    pub code: NnErrorCode,
                    #[component(name = "data")]
                    pub data: wasmtime::component::__internal::String,
                }
                impl core::fmt::Debug for NnError {
                    fn fmt(
                        &self,
                        f: &mut core::fmt::Formatter<'_>,
                    ) -> core::fmt::Result {
                        f.debug_struct("NnError")
                            .field("code", &self.code)
                            .field("data", &self.data)
                            .finish()
                    }
                }
                const _: () = {
                    assert!(
                        12 == < NnError as wasmtime::component::ComponentType >::SIZE32
                    );
                    assert!(
                        4 == < NnError as wasmtime::component::ComponentType >::ALIGN32
                    );
                };
                /// Flattened `wasi:io/error.error`. Upstream `error` is also a resource;
                /// its only accessor is `to-debug-string()`, captured here in `message`.
                /// `source` names the call that produced the failure (e.g.
                /// `"input-stream.read"`) so triage doesn't depend on call-site logs.
                #[derive(wasmtime::component::ComponentType)]
                #[derive(wasmtime::component::Lift)]
                #[derive(wasmtime::component::Lower)]
                #[component(record)]
                #[derive(Clone)]
                pub struct IoError {
                    #[component(name = "source")]
                    pub source: wasmtime::component::__internal::String,
                    #[component(name = "message")]
                    pub message: wasmtime::component::__internal::String,
                }
                impl core::fmt::Debug for IoError {
                    fn fmt(
                        &self,
                        f: &mut core::fmt::Formatter<'_>,
                    ) -> core::fmt::Result {
                        f.debug_struct("IoError")
                            .field("source", &self.source)
                            .field("message", &self.message)
                            .finish()
                    }
                }
                const _: () = {
                    assert!(
                        16 == < IoError as wasmtime::component::ComponentType >::SIZE32
                    );
                    assert!(
                        4 == < IoError as wasmtime::component::ComponentType >::ALIGN32
                    );
                };
                /// Flattened union of the per-call wasi-webgpu error records
                /// (`request-device-error`, `map-async-error`, `create-pipeline-error`,
                /// `get-mapped-range-error`, `set-bind-group-error`, `unmap-error`,
                /// `write-buffer-error`). Every upstream record is shape-compatible with
                /// `{ kind, message }`, so flattening keeps this arm stable as the
                /// wasi-webgpu surface evolves. `operation` names the originating call
                /// (e.g. `"request-device"`, `"map-async"`), `kind` carries the upstream
                /// kind discriminant rendered as a short ASCII tag, `message` is the
                /// upstream message field (or, where the upstream record has none, the
                /// host-side description of what failed).
                #[derive(wasmtime::component::ComponentType)]
                #[derive(wasmtime::component::Lift)]
                #[derive(wasmtime::component::Lower)]
                #[component(record)]
                #[derive(Clone)]
                pub struct WebgpuError {
                    #[component(name = "operation")]
                    pub operation: wasmtime::component::__internal::String,
                    #[component(name = "kind")]
                    pub kind: wasmtime::component::__internal::String,
                    #[component(name = "message")]
                    pub message: wasmtime::component::__internal::String,
                }
                impl core::fmt::Debug for WebgpuError {
                    fn fmt(
                        &self,
                        f: &mut core::fmt::Formatter<'_>,
                    ) -> core::fmt::Result {
                        f.debug_struct("WebgpuError")
                            .field("operation", &self.operation)
                            .field("kind", &self.kind)
                            .field("message", &self.message)
                            .finish()
                    }
                }
                const _: () = {
                    assert!(
                        24 == < WebgpuError as wasmtime::component::ComponentType
                        >::SIZE32
                    );
                    assert!(
                        4 == < WebgpuError as wasmtime::component::ComponentType
                        >::ALIGN32
                    );
                };
                #[derive(wasmtime::component::ComponentType)]
                #[derive(wasmtime::component::Lift)]
                #[derive(wasmtime::component::Lower)]
                #[component(variant)]
                #[derive(Clone)]
                pub enum EntryError {
                    #[component(name = "ws")]
                    Ws(WsError),
                    #[component(name = "store")]
                    Store(StoreError),
                    #[component(name = "nn")]
                    Nn(NnError),
                    #[component(name = "io")]
                    Io(IoError),
                    #[component(name = "webgpu")]
                    Webgpu(WebgpuError),
                    #[component(name = "runtime")]
                    Runtime(wasmtime::component::__internal::String),
                }
                impl core::fmt::Debug for EntryError {
                    fn fmt(
                        &self,
                        f: &mut core::fmt::Formatter<'_>,
                    ) -> core::fmt::Result {
                        match self {
                            EntryError::Ws(e) => {
                                f.debug_tuple("EntryError::Ws").field(e).finish()
                            }
                            EntryError::Store(e) => {
                                f.debug_tuple("EntryError::Store").field(e).finish()
                            }
                            EntryError::Nn(e) => {
                                f.debug_tuple("EntryError::Nn").field(e).finish()
                            }
                            EntryError::Io(e) => {
                                f.debug_tuple("EntryError::Io").field(e).finish()
                            }
                            EntryError::Webgpu(e) => {
                                f.debug_tuple("EntryError::Webgpu").field(e).finish()
                            }
                            EntryError::Runtime(e) => {
                                f.debug_tuple("EntryError::Runtime").field(e).finish()
                            }
                        }
                    }
                }
                impl core::fmt::Display for EntryError {
                    fn fmt(
                        &self,
                        f: &mut core::fmt::Formatter<'_>,
                    ) -> core::fmt::Result {
                        write!(f, "{:?}", self)
                    }
                }
                impl core::error::Error for EntryError {}
                const _: () = {
                    assert!(
                        28 == < EntryError as wasmtime::component::ComponentType
                        >::SIZE32
                    );
                    assert!(
                        4 == < EntryError as wasmtime::component::ComponentType
                        >::ALIGN32
                    );
                };
                #[derive(Clone)]
                pub struct Guest {
                    run: wasmtime::component::Func,
                }
                #[derive(Clone)]
                pub struct GuestIndices {
                    run: wasmtime::component::ComponentExportIndex,
                }
                impl GuestIndices {
                    /// Constructor for [`GuestIndices`] which takes a
                    /// [`Component`](wasmtime::component::Component) as input and can be executed
                    /// before instantiation.
                    ///
                    /// This constructor can be used to front-load string lookups to find exports
                    /// within a component.
                    pub fn new<_T>(
                        _instance_pre: &wasmtime::component::InstancePre<_T>,
                    ) -> wasmtime::Result<GuestIndices> {
                        let instance = _instance_pre
                            .component()
                            .get_export_index(None, "et:ws-wasi/entry@0.1.0")
                            .ok_or_else(|| {
                                wasmtime::format_err!(
                                    "no exported instance named `et:ws-wasi/entry@0.1.0`"
                                )
                            })?;
                        let mut lookup = move |name: &str| {
                            _instance_pre
                                .component()
                                .get_export_index(Some(&instance), name)
                                .ok_or_else(|| {
                                    wasmtime::format_err!(
                                        "instance export `et:ws-wasi/entry@0.1.0` does \
                                            not have export `{name}`"
                                    )
                                })
                        };
                        let _ = &mut lookup;
                        let run = lookup("run")?;
                        Ok(GuestIndices { run })
                    }
                    pub fn load(
                        &self,
                        mut store: impl wasmtime::AsContextMut,
                        instance: &wasmtime::component::Instance,
                    ) -> wasmtime::Result<Guest> {
                        let _instance = instance;
                        let _instance_pre = _instance.instance_pre(&store);
                        let _instance_type = _instance_pre.instance_type();
                        let mut store = store.as_context_mut();
                        let _ = &mut store;
                        let run = *_instance
                            .get_typed_func::<
                                (),
                                (Result<(), EntryError>,),
                            >(&mut store, &self.run)?
                            .func();
                        Ok(Guest { run })
                    }
                }
                impl Guest {
                    pub fn func_run(
                        &self,
                    ) -> wasmtime::component::TypedFunc<(), (Result<(), EntryError>,)> {
                        unsafe {
                            wasmtime::component::TypedFunc::<
                                (),
                                (Result<(), EntryError>,),
                            >::new_unchecked(self.run)
                        }
                    }
                    /// `async` because wasi:webgpu@0.3.0-rc.2 declares `request-adapter`,
                    /// `request-device` and `map-async` as `async func`: a component can only
                    /// await an async import from an async export, so the entrypoint every module
                    /// implements carries the async ABI even for guests that never await.
                    pub async fn call_run<_T, _D>(
                        &self,
                        accessor: &wasmtime::component::Accessor<_T, _D>,
                    ) -> wasmtime::Result<Result<(), EntryError>>
                    where
                        _T: Send,
                        _D: wasmtime::component::HasData,
                    {
                        let callee = self.func_run();
                        let (ret0,) = callee.call_concurrent(accessor, ()).await?;
                        Ok(ret0)
                    }
                }
            }
        }
    }
}
