# Local-only working data cloned here for development, never committed into this repo.
# External upstream repos cloned here carry their own .git history. Ignore every top-level
# entry except this file and the tracked model-modules/.
/*
!/.gitignore
!/model-modules/
