{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Workspace member Cargo.toml -- must inherit workspace lints",
  "description": "Member crates must include `[lints] workspace = true` to inherit workspace lints.",
  "type": "object",
  "required": ["lints"],
  "properties": {
    "lints": {
      "type": "object",
      "description": "Must be `[lints] workspace = true` to inherit the workspace lint table."
    }
  }
}
