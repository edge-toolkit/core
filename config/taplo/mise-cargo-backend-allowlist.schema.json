{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "mise [tools] -- cargo backend allowlist",
  "description": "`cargo:` builds from source; only tools without a prebuilt + cargo:findutils (aarch64-linux).",
  "type": "object",
  "properties": {
    "tools": {
      "type": "object",
      "propertyNames": {
        "anyOf": [
          { "not": { "pattern": "^cargo:" } },
          { "enum": ["cargo:cargo-expand", "cargo:dart-typegen", "cargo:findutils"] }
        ]
      }
    }
  }
}
