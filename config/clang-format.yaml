# clang-format style for C sources.
# Applied via `mise run clang-format` (and clang-format-check). Passed to
# clang-format as --style=file:<this>. LLVM base with the repo's 120-column
# limit and 4-space indent (matches the C sources).
BasedOnStyle: LLVM
IndentWidth: 4
ColumnLimit: 120
